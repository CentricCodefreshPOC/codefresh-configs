apiVersion: codefresh.io/v1beta1
kind: PromotionFlow
metadata:
  name: test
  annotations:
    description: ""
spec:
  triggerEnvironment: development
  steps:
    - environment: staging
      dependsOn:
        - development
      policy:
        action: commit
        postAction: notify-slack
