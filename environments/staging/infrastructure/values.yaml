postgresql:
  enabled: true
  fullnameOverride: postgresql-staging
  auth:
    postgresPassword: postgres
  primary:
    persistence:
      enabled: false

redis:
  enabled: true
  fullnameOverride: redis-staging
  auth: 
    enabled: false
  master:
    persistence:
      enabled: false
  replica:
    persistence:
      enabled: false