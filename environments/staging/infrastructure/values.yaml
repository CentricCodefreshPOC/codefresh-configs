defaultStorageClass: gp2

postgresql:
  enabled: true
  fullnameOverride: postgresql-development
  auth:
    postgresPassword: postgres
  primary:
    persistence:
      enabled: false

redis:
  enabled: true
  fullnameOverride: redis-development
  auth: 
    enabled: false
  master:
    persistence:
      enabled: false
  replica:
    persistence:
      enabled: false