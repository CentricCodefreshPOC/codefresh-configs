# Default values for result-app.
# This is a YAML-formatted file.
# Declare name/value pairs to be passed into your templates.
# name: value
image:
  repository: aiforaplatform/codefresh-poc-svc-a
  tag: 1.0.0
replicaCount: 1
domain: cpi.centricsoftware.com
analysis:
  curl:
    successCode: 200
